using Machine.Specifications;

namespace FileStorage.Tests
{
    [Subject("MimeTypes")]
    public class When_a_new_activity_is_created
    {
        //Establish context = () =>
        //{
        //    AzureStorageSettings settings = new AzureStorageSettings(string.Empty, string.Empty);
        //    // IFileStorageRepository repo = new AzureFileStorageRepository();
        //    //repo.Upload();
        //    //repo.GetFileUri();

        //    var asd = new FileInfo("gg").Extension;
        //};

        ////Because of = () => item = new Activity(streamId, id, body, author);

        //It should_have_id = () => 1.ShouldEqual(1);
    }
}
